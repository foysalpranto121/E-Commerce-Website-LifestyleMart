services:
  - type: web
    name: lifestylemart
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python database_setup.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
